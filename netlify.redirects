# Redirects dari netlify.com ke domain kustom
# Uncomment jika Anda menggunakan domain kustom
# http://yoursitename.netlify.com/* https://custom-domain.com/:splat 301!

# Next.js handles routing internally, these are just for edge cases
/CERTIFICATION\ CYBER\ SECURITY/* /api/serve-certification 200
/api/* /api/:splat 200

# SPA fallback
/* /index.html 200 